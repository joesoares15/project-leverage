# Generated research data

`anyrbona53.json` is generated by `scripts/build_anyrb_data.py` during the GitHub Pages workflow.

Do not hand-edit the generated JSON. The website intentionally reads a same-origin bundled file so the research module has no browser-time dependency on nflverse, GitHub release assets, CORS proxies, Netlify rewrites, or serverless functions.
